services:
  - type: web
    name: amr-fleet-coordination
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m live.orchestrator --robots 3
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: ROBOTS
        value: 3
